import { defineConfig } from "vite";

export default defineConfig({
    plugins: [],
    resolve: {
        alias: {
            path: "path-browserify",
        },
    },
    base: "/semester-project-2-olemartin/",
});
